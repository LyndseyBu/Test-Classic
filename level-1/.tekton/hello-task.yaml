apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: hello-task
spec:
  steps:
    - name: echo
      image: ubuntu
      command:
        - echo
      args:
        - "hello world"
    - name: echo3
      image: ubuntu
      onError: continue
      command:
        - echo
      args:
        - "hello world4"
     - name: echo4
      image: ubuntu
      onError: continue
      command:
        - echo
      args:
        - "hello world3"
